apiVersion: v1
kind: Service
metadata:
  name: {{ include "electricity-comparison.fullname" . }}
  labels:
    {{- include "electricity-comparison.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "electricity-comparison.selectorLabels" . | nindent 4 }}
